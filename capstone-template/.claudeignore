# Sensitive local files
.env
*.pem
*.key
*.secret

# Data locations that should stay out of model context if students add them
data/raw/
data/restricted/
data/*/administrative/

# Zotero import seed: students import this into Zotero, Claude should query Zotero instead
zotero/*.bib
